[Maybe Fix] Encryption tool has supposed issues with _ = <-- The async function

- Check to see if tests such as password validation on the pages
would be a good idea.
- HAVE the automated TESTS run when uploading? maybe attempting
to commit or push to git or GitHub?

Have the user sent to a login page (/login) if they aren't logged in,
this is where they will log in using a username and password that is
securely stored within a database.

The C# program must be password protected,
requiring the user to enter a username and password first to
gain access to the main application,
where the user's credentials must be contained within an
SQL database.

[Do] - Comments where required
[Probably Done] - Camel case
[Probably Done] - Indentation
[DONE] - Meaningful variable/function naming conventions
[DONE] - Have meaningful variables/functions.
[DONE] - Split them up like input username, check username.
[DONE] - Don’t make a single function do too much.
